import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    
    // Turbopack is no longer supported, so this entry is removed
  }
};

export default nextConfig;
